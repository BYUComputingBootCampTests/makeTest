CXX=g++
CFLAGS=-Wall
SRCS=$(wildcard *.cpp)
HDRS=$(wildcard *.h)
OBJS=$(SRCS:.cpp=.o)
TARGET=tree

$(TARGET): $(OBJS)
	$(CXX) $(CFLAGS) -o $@ $^
	echo $(HDRS) >> compiled.txt

%.o: %.cpp $(HDRS)
	$(CXX) $(CFLAGS) -c $<
	echo $< >> compiled.txt

clean:
	rm $(TARGET) *.o compiled.txt