CC=g++
TARGET=tree
SRCS=$(wildcard *.cpp)
OBJS=$(SRCS:.cpp=.o)

$(TARGET): $(OBJS)
	$(CC) -o $(TARGET) $(OBJS)
	@echo "$(OBJS)" > compiled.txt

%.o: %.cpp %.h
	$(CC) -c $<

clean:
	rm $(TARGET) $(OBJS) compiled.txt