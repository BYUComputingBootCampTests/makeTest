CC=g++
CFLAGS=-Wall
SRCS=$(wildcard *.cpp)
HDRS=$(wildcard *.h)
OBJS=$(SRCS:.cpp=.o)
TARGET=tree

$(TARGET): $(OBJS)
	$(CC) $(CFLAGS) -o $@ $^

%.o: %.c $(HDRS)
	$(CC) $(CFLAGS) -c $<

clean:
	rm $(TARGET) *.o compiled.txt